
# MySQL local credentials
spring.datasource.url=jdbc:mysql://localhost:3306/desi_restro_db?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=desi_restro
spring.datasource.password=DesiRestro@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JWT (dev secret NOT for production)
jwt.secret=+O+lT01PYSxnXOMeI0qfYNyOR/eS4VWSlTNTLADH6DIWxBR5QCuuw0aMaNAMfaw8/Q7k5bBFjR9IFAca4Bth4Q==

# CORS allow local frontend
app.allowed-origins=http://localhost:3000,http://localhost:5173

# Logging verbose for development
logging.level.com.dts.restro=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.flywaydb=DEBUG

# Swagger visible in dev
springdoc.swagger-ui.enabled=true

# H2 console (if ever needed for quick testing)
# spring.h2.console.enabled=true

# Password Reset
app.reset-token-expiration-minutes=30
app.frontend-url=http://localhost:3000

# Mail (dev configure with MailHog or real credentials)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
